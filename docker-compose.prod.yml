version: '3.9'

services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: nginx_sportshop_prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.prod.conf:/etc/nginx/nginx.conf:ro
      - certbot_certs:/etc/letsencrypt:ro
    depends_on:
      - frontend
      - admin
      - backend
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend_sportshop_prod
    expose:
      - "3000"
    environment:
      - VITE_API_URL=/api
    restart: always

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    container_name: admin_sportshop_prod
    expose:
      - "3001"
    environment:
      - VITE_API_URL=/api
    restart: always

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend_sportshop_prod
    expose:
      - "8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_URL: jdbc:mysql://database:3306/sportshop?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid
      DB_USERNAME: root
      DB_PASSWORD: ${DB_PASSWORD:-curso}
      JWT_SECRET: ${JWT_SECRET:-change-in-production}
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
    depends_on:
      database:
        condition: service_healthy
    restart: always

  database:
    image: mysql:8.4
    container_name: database_sportshop_prod
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-curso}
      MYSQL_DATABASE: sportshop
      TZ: Europe/Madrid
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: always

volumes:
  mysql_data:
    name: mysql_sportshop_prod_data
    driver: local
  certbot_certs:
    name: certbot_sportshop_certs
    driver: local