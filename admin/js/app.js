/* app.js - Converted from App.tsx */
// In vanilla JS, routing is handled by the browser's navigation between HTML files.
// Each page (dashboard, pedidos, etc.) is its own .html file.

// Global app configuration or state can go here.
export const AppConfig = {
    version: '1.0.0',
    name: 'Sportshop Admin'
};

console.log('App logic loaded');
