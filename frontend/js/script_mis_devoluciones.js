const content = document.querySelector('#page-content');

if (content) {
  content.innerHTML = `
    <p>Vista estática convertida desde la versión React original.</p>
    <p>La lógica específica de esta pantalla debe mantenerse en este archivo JavaScript.</p>
  `;
}

export const pageName = 'MyReturns';

