document.getElementById('registerForm').addEventListener('submit', function(e) {
    // Si se desea manejar por AJAX en el futuro, descomentar e.preventDefault()
    // e.preventDefault();
    
    console.log('Registering user...');
    // Se puede añadir validación extra aquí si es necesario
});
